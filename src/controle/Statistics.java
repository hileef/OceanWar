package controle;

public class Statistics {

//	private static long currentCount;
	
	public static void checkPoint(String checkpoint) {
//		long d;
//		if((d = (System.currentTimeMillis() - currentCount)) > 0)
//			System.out.println("STATS CHECKPOINT : " + checkpoint + " : " + d);
//		currentCount = System.currentTimeMillis();
	}
	
}
